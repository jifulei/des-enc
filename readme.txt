本程序用于实现DES加密算法，给定16进制的64bit明文和密钥，生成16进制的64bit密文。

利用字符串数组进行明文和密钥的输入，利用整形数组存储中间数组和计算过程，最终密文用整形数组存储。

明文和密钥写在只读文件input.txt中，明文和密钥以空格分格（见文件input.txt）。
程序结束后将明文密钥密文写入只写文件output.txt中。
本程序提供的范例如下：
明文4e6f772069732074  密钥0123456789abcdef  密文3fa40e8a984d4815